# Backend API Configuration
API_URL={{ api_url | default('YOUR_API_URL') }}
API_KEY={{ api_key | default('YOUR_API_KEY') }}
DATABASE_URL={{ database_url | default('YOUR_DATABASE_URL') }}
BACKEND_URL=http://{{ ansible_host }}:8080

# Application Environment
NODE_ENV={{ environment | default('production') }}
PORT=8080

# Frontend Configuration
FRONTEND_PORT=3000
NGINX_PORT=80 